version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install -g pnpm
        - export PATH=$PATH:`npm bin -g`
        - pnpm install
    build:
      commands:
        - pnpm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*

backend:
  phases:
    build:
      commands:
        - npm install -g pnpm
        - export PATH=$PATH:`npm bin -g`
        - pnpm install
        - amplify pull --yes
    postBuild:
      commands:
        - amplify push --yes
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
